<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>

    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>

    <key>CFBundleDisplayName</key>
    <string>GoalShare</string>

    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>

    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>

    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>

    <key>CFBundleName</key>
    <string>GoalShare</string>

    <key>CFBundlePackageType</key>
    <string>APPL</string>

    <key>CFBundleShortVersionString</key>
    <string>$(FLUTTER_BUILD_NAME)</string>

    <key>CFBundleSignature</key>
    <string>????</string>

    <key>CFBundleVersion</key>
    <string>$(FLUTTER_BUILD_NUMBER)</string>

    <key>LSRequiresIPhoneOS</key>
    <true/>

    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>

    <key>UIMainStoryboardFile</key>
    <string>Main</string>

    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- INTENTIONALLY REMOVED CADisableMinimumFrameDurationOnPhone.
         That key opts the app into 120Hz ProMotion. On ProMotion devices that
         triggers Flutter's -[FlutterViewController createTouchRateCorrectionVSyncClientIfNeeded]
         -> -[VSyncClient initWithTaskRunner:] which null-derefs and crashes on launch
         (Flutter #183900). Without the key the display runs at 60Hz, that code path is
         skipped (it early-returns when refresh rate <= 60), and the crash is avoided. -->

    <key>UIApplicationSupportsIndirectInputEvents</key>
    <true/>

    <!-- Camera Permission -->
    <key>NSCameraUsageDescription</key>
    <string>GoalShare uses the camera to let you upload progress photos for your goals.</string>

    <!-- Photo Library Read Permission -->
    <key>NSPhotoLibraryUsageDescription</key>
    <string>GoalShare needs access to your photo library so you can select and upload images.</string>

    <!-- Photo Library Write Permission -->
    <key>NSPhotoLibraryAddUsageDescription</key>
    <string>GoalShare saves images to your gallery when you download or export content.</string>

    <!-- Encryption compliance — app uses only standard HTTPS, no custom encryption -->
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>

</dict>
</plist>