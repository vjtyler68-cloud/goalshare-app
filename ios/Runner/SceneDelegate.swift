import UIKit

// Intentionally unused. The app runs the classic UIApplication/app-delegate
// lifecycle (no UIApplicationSceneManifest in Info.plist), which iOS 26 honors
// when the app is built with the iOS 18 SDK (Xcode 16). Kept as a harmless stub
// so the Xcode project reference stays valid without project-file surgery.
class SceneDelegate: UIResponder, UIWindowSceneDelegate {
  var window: UIWindow?
}
